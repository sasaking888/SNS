# 依頼: 3タイルのデザイン再検討 ＋「選ばれる理由」との内容重複解消

**必ず `GROK-RULES.md` と `CLAUDE-HANDOFF.md` を先に読むこと。** 数字・法令文言・禁止語のルールはこの依頼書より優先される。

---

## 背景・経緯

ヒーロー写真の直後にある3タイル（`#badges`、ネイビー背景、`.badge-card` × 3）について、オーナーとClaudeで検討した結果、2つの課題が見つかった。**方針はまだ確定していない。案を複数出してオーナーに選んでもらう想定で作業すること。**

### 課題1: アイコンの意味が合っていない

現在のアイコン（`index.html` 86〜136行目付近）:

| タイル | アイコン | 問題 |
|---|---|---|
| 省スペース（0.5畳） | 枠線（ビューファインダー） | 概ね妥当 |
| 高回転（10分） | 時計 | 概ね妥当 |
| 高品質（33万人+の施術実績） | **盾（シールド）** | **盾は「保護・安全」を表す記号で、「実績人数」と意味がつながっていない** |

さらに全アイコンに黄色い小さいチェックマークバッジ（`.badge-card__check`）が重ねてある。これは以前オーナーが提示した他社（カイタク）の参考デザインをそのまま持ち込んだもので、色は変えたが「借り物っぽさ」が残っている、という指摘も出ている。

**依頼**: 3つ目の「実績人数」に合うアイコン（例: 人物のグループ、グラフ、トロフィー等）への差し替え案。あわせて、チェックマークバッジを残すか外すか・見た目を変えるかの案も出してほしい。

### 課題2: `#badges` と `#reasons` の POINT 02 が内容重複している

| | `#badges`（3タイル） | `#reasons`（選ばれる理由） |
|---|---|---|
| 省スペース | 「0.5畳」「たった0.5畳分のスペースでOK」 | POINT 02「たった0.5畳分のスペースで施術可能」 |
| 高回転 | 「10分」「おひとり約10分・1時間で約5名」 | POINT 02「おひとり約10分、1時間で約5名さまに提供できます」 |

見出し・本文とも実質同じ内容が2回出てくる。`#reasons` の POINT 01（施術実績）と POINT 03（福利厚生・採用）は独自の内容を持つに至ったが、**POINT 02だけがタイルの再掲になっている**。

`#reasons` の POINT 02 の現在の全文（`index.html` 219〜225行目）:
> たった0.5畳分のスペースで施術可能。おひとり約10分、1時間で約5名さまに提供できます。日々の業務で疲れた身体に対し、オフィス整体で肩こり、首の痛み、頭痛、腰痛など気になる部分をケアし、従業員様の健康維持をサポートします。

**依頼**: 以下のどちらかの方向で案を出してほしい（オーナーが選ぶ）。

- **案A**: POINT 02の前半（スペック再掲部分）を削除し、後半の「肩こり・首の痛み・頭痛・腰痛のケア」だけを主軸にした別角度の文章に書き直す（見出しも「省スペース・高回転」から変更）
- **案B**: `#reasons` を3枚から2枚に減らし、POINT 02（省スペース・高回転）を削除する。タイル側で既に伝わっているという判断

どちらも**「絶対に効果がある」といった断定は禁止**（GROK-RULES §2参照。「治る・治療・改善します」はNG、「ケアする／整える」を使う）。

---

## 触ってよい範囲

- `index.html`: `#badges` 内のSVGアイコン3つ、`.badge-card__check` の扱い、`#reasons` の POINT 02 のマークアップ・文言
- `styles.css`: 上記に対応するスタイル（`.badge-card__icon`、`.badge-card__check` 関連）

## 触ってはいけないもの

- 数字（0.5畳／10分／33万人+）は**変更禁止**（GROK-RULES §2の確定数字表参照）
- タイルの位置（ヒーロー写真の直後、3列固定）、`.hero--tiles` のネイビー背景、数字を主役にした見せ方（大きい黄色文字）は**維持**。今回変えるのはアイコンの中身と、文言の重複解消のみ
- `#voices`（お客様の声）・`.phone-cta`（電話番号セクション）など、今回の依頼と関係ないセクションには触らない

---

## 自己検証（提出前に必ず実施）

- 320 / 375 / 1200px で実際に表示して確認（横スクロールなし、崩れなし）
- 新アイコンのSVGは既存の他アイコンとサイズ・線の太さ（`stroke-width: 1.5`）を揃える
- 変更後の数字（0.5畳／10分／33万人+）が本文中の数字と一致しているか再確認
- 禁止語（マッサージ／治る・治療／必ず・絶対）が入っていないか確認

## 提出のしかた

- 変更した箇所の差分のみ報告
- アイコン案・POINT 02案は**複数案を提示**（1案に決め打ちしない。オーナーが選ぶため）
- どの幅で何を確認したか明記
